# Optional overrides used by the IaC scripts and systemd service.
# The default service listens on all VM interfaces so the host can open the UI.

HERMES_UI_HOST=0.0.0.0
HERMES_UI_PORT=8765
HERMES_HOME=$HOME/.hermes
HERMES_STATE_DB=$HOME/.hermes/state.db
HERMES_SESSION_DIR=$HOME/.hermes/sessions
